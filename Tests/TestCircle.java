package Tests;

import geom.entities.Circle;
import geom.entities.GeometricObj;

public class TestCircle {

    public static void main(String[] args) {
        GeometricObj[] circles = { new Circle(1.5), new Circle(0.5) };
    }
}
